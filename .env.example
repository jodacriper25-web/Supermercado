# .env.example - Copiar este archivo a .env y configurar los valores

# DEBUG MODE
# En producción: FALSE
DEBUG=True

# SECURITY
# Generar una clave secreta: from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())
SECRET_KEY=your-secret-key-here-change-in-production

# ALLOWED HOSTS
# En producción, agregar tu dominio
ALLOWED_HOSTS=127.0.0.1,localhost

# DATABASE (Opcional: solo si usas PostgreSQL)
# DATABASE_URL=postgresql://user:password@localhost:5432/supermercado

# EMAIL (Opcional: para notificaciones)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
